on:
  push:
    branches:
    - master

jobs:
  notify:
    uses: ./.github/workflows/notify.yaml
    secrets: inherit

  codeql-analysis:
    uses: ./.github/workflows/codeql.yaml
    permissions:
      actions: read
      contents: read
      security-events: write